// Barrel for motion primitives — import from "@/components/motion".
// These wrappers are client boundaries, so server components can animate
// by wrapping (never by calling motion APIs directly).
export { FadeIn } from "./FadeIn";
export { Reveal } from "./Reveal";
