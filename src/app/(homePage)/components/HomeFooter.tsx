import { PublicFooter } from "@/components/layout/PublicFooter";

/** Homepage footer — the universal public footer (kept for existing imports). */
export function HomeFooter() {
  return <PublicFooter />;
}
